// Theme: Console.log()
// some song by Doors...once, twice, thrice (String substitution version)

var loveCounter = ['once', 'twice', 'thrice'];
for (var i=0; i<3; i++) {
    console.log("Love me %d times, %s, love me %s today", i+1, "baby", loveCounter[i]);
}
console.log('What is the name of the song?');